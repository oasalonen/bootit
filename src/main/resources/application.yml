app:
  name: bootit
  version: '1.0'

server:
  port: 8001

management:
  endpoint:
    prometheus:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  path: 'log/'